import HeroDescription from "./HeroDesciption";
import HeroFigure from "./HeroFigure";

const Hero = (props) => {
    return (
        <sectioin className="hero">
            <HeroDescription />
            <HeroFigure />
        </sectioin>
    )
}

export default Hero;